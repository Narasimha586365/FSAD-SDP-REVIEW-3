// Authentication is handled only by the backend API.
// This file remains as a safe placeholder so no frontend passwords or
// credential comparisons are exposed in client-side code.

export const validateAdmin = () => null;

export const getAdminData = () => null;
